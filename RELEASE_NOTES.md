# v0.1.77

- 修复高级编辑"进入后退出卡死"：根因是 AI Studio 网关对 `/userdata` 偶发"200 空列表"假阳性，App 误以为服务器支持云端工作流，前端就把"云端工作流开关"翻成 true；退出高级编辑时前端去服务器按路径加载 `ComfyUI_3放大.json`（中文路径必 400），读取结果连续失败、怎么点都退不出去。修复：① 列表连续**两次**成功才把开关置 true（直连服务器稳定 200 无感，AI Studio 的 200/404 交替永远到不了 2 次，开关保持 false 走内容直载）；② 就算开关误置 true，前端"恢复被编辑的工作流"也不再硬报错——切换失败一律降级为"直接导出当前画布"（画布内容本来就是 App 灌进去的），高级编辑退出永不卡死。
- 修复生成按钮"一会灰一会紫"反复横跳：AI Studio 平台每 11-12 秒自动重载一轮页面，每次重载桥接都要重新恢复，恢复期间按钮禁用。以前恢复失败要等下一次页面加载（最长 10 秒+），灰色窗口被拉满。现在恢复在 **40 秒窗口内每秒自动重试**，前端初始化完成的瞬间（通常 2-5 秒）就恢复成功并保持到下一次重载；恢复触发延迟 0.8s→0.2s、就绪轮询 500ms→200ms，灰色窗口压缩到最短。
- 渲染进程崩溃恢复超时从 90 秒砍到 30 秒，不再让用户对着灰按钮干等。

# v0.1.76

- 修复参数页/快捷生图进度条与实际任务进度不符：ComfyUI 的 `progress` 消息只反映**采样器步数**（value/max），排队、加载模型、VAE 解码等阶段根本没有进度消息，旧版会停在 0%/100% 造成"进度不对"的错觉。现在分阶段展示：排队/加载/解码阶段显示**不确定进度条**（动画），采样阶段才显示真实百分比；执行开始文案改为"正在加载模型"，不再假装 0%。
- 修复图片信息页"生图耗时"不准确：旧版只显示服务器记录的**执行耗时**（execution_start→execution_success），AI Studio 这类平台排队时间长，用户体感严重偏短。现在拆分显示两行——**总耗时（含排队）**（App 本地记录提交→完成时刻，同源无时钟偏差）+ **执行耗时**（服务器记录）。
- 生成按钮"莫名其妙变灰"加了解释：AI Studio 平台每十几秒自动重载页面，重载瞬间桥接在恢复、按钮短暂禁用。现在按钮下方会显示"网页正在重载，正在恢复连接…（几秒内恢复）"，不再一头雾水；页面重载后的恢复触发延迟也从 1.5s 缩到 0.8s。
- UI 细节：参数页任务卡片加了同步图标，进度条不确定/确定两种状态；图片信息弹窗加分隔线分组、文件名加大字号；参数页生成按钮在生成中显示"生成中…"（与快捷页一致）。

# v0.1.75

- 修复百度 AI Studio 上"工作流能读、但参数页/快捷生图的生图按钮全灰"的问题（v0.1.74 实测日志定位）。根因链：AI Studio 平台会**每十几秒自动重载一轮页面**（平台行为，实测 15:17:24 → 15:18:56 共 8 个轮次），页面过大又导致渲染进程频繁崩溃重建。重建后 App 走"恢复桥接"流程，但旧逻辑用 90 秒长超时死等"工作流状态稳定 750ms"——该平台 `syncWorkflows()` 挂住不返回 + 重载循环让稳定窗口永远等不到，恢复超时后还 `scheduleReconnect()` 主动 reload 页面，跟平台重载叠成"重载风暴"。结果 `bridgeReady` 长期为 false，三个入口（参数页生成、高级编辑、快捷生成）全部禁用。
- 修复三件事：① 页面**每完成一次加载**都自动快速恢复桥接（不等渲染进程崩溃，恢复窗口从几分钟压缩到几秒）；② READY_SCRIPT 放宽 ready 判定——画布上已有工作流内容就直接放行（不依赖工作流列表稳定），稳定窗口 750ms 降到 200ms；③ 页面重载触发的快速恢复失败**不再主动重连**（HTTP 是通的，重连 reload 只会加剧重载风暴），等下一次页面加载完成自动重试；仅渲染进程崩溃路径连续失败 3 次才回退完整重连。
- 快速恢复超时 15 秒（渲染进程崩溃路径仍 90 秒），带防重入，两个恢复入口不会并发打架。

# v0.1.74

- 修复百度 AI Studio 上"列表探测被假阳性骗过"的问题（v0.1.73 实测日志定位）：AI Studio 网关对 `/userdata` 是"半死不活"的——列表接口偶尔返回 200 空列表（被 App 误判成"支持云端存储"），但单文件读写必败（查询参数被网关剥掉导致 `400 Directory not provided`、中文路径 `400 Invalid char in url path`）。一旦被误判，加载工作流时前端就会去按路径读服务器文件然后失败。现在两道保险：① 工作流列表**任何原因读不到**都直接按"不支持云端存储"处理，不再依赖"是否判为不支持"这个判定；② 前端脚本里"服务器分支"包了兜底——按路径读取**任何一步失败**都自动退回用手机里已有的内容直接加载，探测再怎么漏网都不会把加载搞死。
- 实测补充：已直接验证这台 AI Studio 网关的行为（带 Cookie 请求）：`/system_stats` 正常、`/userdata` 无参数返回 400 Directory not provided、`/userdata/workflows` 返回 403、中文路径返回 400——`/userdata` 的读写在这类平台上**根本不可用**，App 不会再依赖它。

# v0.1.73

- 修复百度 AI Studio 上"工作流加载/导入失败"的时序问题（本轮日志实测定位）：连接服务器时会把"云端工作流是否可用"的开关复位，而复位到探测完成之间存在窗口——期间点开工作流，前端会去按路径读取服务器文件，中文文件名（如 `ComfyUI_3放大.json`）被百度网关以 `400 Invalid char in url path` 打回，加载直接失败。现在连接后默认按"不支持云端存储"处理，只有探测确认支持（直连服务器/CloudStudio）才切回服务器列表模式；加载永远有"内容直载"兜底，不再依赖服务器路径。
- 百度 AI Studio 上导入、保存、另存为不再发必败的服务器请求：这类平台不开放 `/userdata`，以前每次导入/保存都要先白跑一遍列表查询和写入（中文文件名还会多刷一条 400 报错），失败后才降级。现在探测确认不支持时直接本地化——导入存本机、保存存本地草稿、另存为本机副本，功能不中断，日志也干净了。

# v0.1.72

- 修复批量出图"注入了个寂寞"：以前靠"节点名字里含不含 Sampler"来猜该往哪里写 `batch_size`，而对照 ComfyUI 官方源码（nodes.py）核实过，KSampler / KSamplerAdvanced / SamplerCustom 这些采样器的输入定义里**根本没有 `batch_size`**——写进去的字段被服务端静默忽略（execution.py 只读取节点声明过的输入），却让调用方以为批量注入成功了。同时名字里带 "Sampler" 的非采样器（如 KSamplerModel、各种第三方节点）也会被误写垃圾字段。现在改为只写**真正有 `batch_size` 输入的节点**（latent 源节点一类），未来新增的节点类型也能自动支持，不再靠猜。
- 修复退出 App 时主线程可能被磁盘写入卡死（ANR）：`onCleared()` 里用 `runBlocking` 等草稿落盘，存储慢的手机在系统回收进程时几十毫秒就超时。改为在进程级协程里异步兜底保存，写不进去顶多丢这次草稿，绝不用主线程响应时间去换。
- 提示词历史弹窗改用 `?.let` 写法，去掉 `!!` 强解包（纯代码整洁，无行为变化）。

# v0.1.71

- 修复后台任务监控"无限轮询"：轮询任务状态时遇到 404/403 这类重试也不会变好的错误，以前被当成"任务还没完成"悄悄吞掉，连续失败上限因此永远触发不了——AI Studio 拿不到 Cookie 时一个任务能刷出 80 次请求。现在如实计入失败，永久性错误 10 秒内放弃并发通知，5xx 与网络抖动仍给足重试机会。
- 修复 IPv6 地址下的 HTTP Basic 自动登录：比对主机名时按 `:` 截断，把 `[::1]:8188` 切成了 `[`，和预期值永远对不上，填了账号密码也不会自动登录。现在两边都先归一化（去端口、去方括号、统一小写）再比。
- 修复取消任务会误伤其他任务：原生 ComfyUI 并没有 `/api/jobs/{id}` 接口，取消运行中的任务必然 404 后回退到 `/interrupt`，而它中断的是"当前正在执行"的那一个——目标任务若已跑完，这一刀就砍在别人的任务上。改为先发 `/queue` delete，只有确认目标仍在运行队列中才中断，确认不了就不动手。
- 登录凭据不再随系统备份上云：服务器 Cookie 等同于平台账号密码，以前会被云端备份（Google 云端硬盘）和新机迁移一起带走，现已排除在备份范围外。代价是换机不会自动恢复服务器列表，需要重新填一次。
- 修复读取 PNG 内嵌工作流时的"解压炸弹"：解压过程没有任何体积上限，精心构造的几十 KB 图片就能把内存吃光导致闪退。现在解压上限 8MB（正常工作流只有几百 KB）。
- 修复 App 内"检查更新"完全不工作：构建一直被发布成名叫 `latest` 的预发布版本，而 GitHub 的"最新 Release"接口只认正式的非预发布版本，直接返回 404；再加上标签名不是版本号、缺少 `.sha256` 校验文件，就算拿到了也会被跳过。现在改为发布正式的版本标签（带校验文件），`latest` 保留为固定下载链接。
- 修复更新下载完却装不上：构建每次都现场生成一把新的签名密钥，而安装前会比对签名证书，对不上就拒绝安装。现在签名密钥跨构建复用，升级不再需要卸载重装。
- 应用内"检查更新"改为指向本仓库，不再去拉上游 Release——否则一安装就把这边的改动全冲掉。
- 补齐 v0.1.58 ~ v0.1.70 的更新记录（此前这份文档停在 v0.1.57）。

> 升级提示：从 v0.1.70 及更早版本升到 v0.1.71 时签名与旧版不同，需要先卸载旧版再安装。装好 v0.1.71 之后，后续版本就能在 App 内直接更新了。

# v0.1.70

- 工作流列表改为优先显示本机真正存过内容的工作流：百度 AI Studio 这类反向代理不转发 `/userdata`，云端列表读不到时，以前只能显示几个读不出内容的"最近浏览"占位条目，点开必报错。现在用本机快照顶上，读不到的条目给出"回工作流页导入一次"的具体指引。
- 修正登录墙的报错文案：反向代理的登录墙常以 403 + 普通网页错误页出现，以前被误报成"该服务器不支持此接口"，纯属误导。现在会明确提示"需要登录或登录已失效"。登录页不再参与重试——Cookie 不会因为多试两次就自己出现。
- 云端工作流接口遇到中文路径报 `HTTP 400 Invalid char in url path` 时，自动改用双重编码重试一次。

# v0.1.69

- 修复加载工作流卡死：反向代理不转发 `/userdata`，ComfyUI 前端的 `syncWorkflows()` 会永远挂住不返回，导入工作流的流程整个僵住。现在给前端同步加了超时闸门，并在探测到服务器不支持时彻底跳过这一步。
- 新增工作流正文本机快照：从服务器读成功过的工作流会在本机留一份，服务器读不到时自动回退，最多保留 30 份。
- 修复认证 Cookie 记不住：服务器地址按归一化后的形式匹配（此前带 `:443` 与不带端口会被当成两台不同服务器），粘贴后停手 800ms 才落盘，不再每敲一个字符就写一次磁盘。
- Cookie 输入框最多 4 行、提示词输入框最多 6 行，内容再多也不会把页面撑变形。

# v0.1.68

- 兼容百度 AI Studio、CloudStudio 这类反向代理服务器：能识别代理返回的 HTML 错误页，不再把"服务器不支持此接口"和"需要登录"混为一谈，按平台能力自动降级可用功能。

# v0.1.67

- 修复图片查看页黑底、分辨率显示和"复制种子"。
- 切换服务器时兜底保留当前工作流，不再被清空。
- 后台任务监控增加连续失败上限。
- 内部：新增 API prompt 原生解析器与参数写回构建器（为后续 LoRA 批量对比功能做准备，尚未接入界面）。

# v0.1.66

- 修复云端任务、图片、工作流保存时的一连串 404。
- 自动保存不再把图片写进系统相册。

# v0.1.65

- Cookie 改为逐段注入 WebView，修复粘贴 Cookie 后仍被甩回登录页的问题。

# v0.1.64

- 按用户反馈精简：移除 App 内置登录窗口，只保留手动填写 Cookie 的输入框。

# v0.1.63

- 新增 App 内登录，不用再去浏览器复制 Cookie。

# v0.1.62

- 支持反向代理认证 Cookie，修复百度 AI Studio 连接失败。

# v0.1.61

- 修复快捷生图页卡死、API 格式工作流导入和图片查看页的若干显示问题。
- 新增自动保存与种子模式。

# v0.1.60

- 新增快捷生图页，支持在页内二次修改服务器地址。
- 修复图片查看页底部栏。

# v0.1.59

- 支持 API 格式工作流、批量出图、自定义保存目录。
- 修复节点识别。

# v0.1.58

- 清洗多行粘贴的服务器地址，避免复制云平台控制台地址时因换行导致解析崩溃。

# v0.1.57

- 修复重启 App 后，在连接页点击另一个服务器连接时，地址输入框会跳回上一次连接地址的问题：现在连接过程中不会再被后台数据刷新覆盖你正在输入或刚设置的新地址。

# v0.1.56

- 修复高级编辑在“当前工作流没有已连线的输出节点”时点关闭会失败、导致改动丢失的问题：现在会正常退出并保留编辑内容，参数页提示连接输出节点后再进入。
- 任务列表为已完成的生成任务显示具体耗时（运行中/等待中的任务没有结束时间戳，暂不显示）。
- 工作流页单击只预读取参数、不再进入参数页；双击或点击顶部“打开参数”才会进入，最近打开列表只记录真正打开过的工作流。
- 修复生成完成并保存后，绿色执行框偶尔停留在上一个节点的问题。

# v0.1.55

- 修复接管任务后参数页没有绿框和进度的问题：运行节点、进度和绿框更新原来只对本 App 提交的任务生效，现在接管（或正在跟踪）的外部任务同样实时更新，参数页会像正常生成一样显示当前执行部件和进度。
- 接管的任务也会启动后台任务监控，切到后台或锁屏也能看到进度通知。

# v0.1.54

- 修复接管任务后参数页为空的问题：电脑浏览器等提交的任务不带工作流路径，现在会直接解析任务内嵌的执行工作流图来打开参数页；接管后自动切换到参数页并显示该工作流的参数。
- 任务列表解析同时保留任务内嵌的工作流内容，供接管使用。

# v0.1.53

- 任务接管改为手动接管：任务页里点运行中/等待中的任务（或点"接管"），会立即接管该任务并自动打开它对应的工作流，参数页跟着这个任务走；接管不再限于"本 App 提交"的任务，电脑浏览器提交的任务也能点开接管。
- 任务卡片显示"点击接管此任务并打开对应工作流"，正在跟踪的任务会标记"正在跟踪中"。

# v0.1.52

- 更新下载改为"按网络实测自动选源"：打开 App 检查更新时，同时探测 GitHub 与国内镜像（ghfast、ghproxy），哪个先响应就用哪个；下载 APK 前也会并发实测各镜像延迟，自动选择最快可达的源，并显示来源与延迟。
- 设置页新增下载进度显示：下载中会显示百分比与来源节点，下载完成后自动校验 SHA-256、包名和签名并引导安装。
- GitHub API 检查也加了国内镜像兜底，api.github.com 被墙或超时也能检测到新版本。

# v0.1.51

- 本地草稿改为"只保存改变的内容"：参数页的草稿只记录你改过的字段（值、可见性、分组、顺序），打开工作流时始终以服务器版本为基础，再把改动叠加回去；即使本地数据被写乱，键对不上的改动也会被忽略，不可能再混入其他工作流的整份数据。
- 高级编辑器修改过节点结构（增删节点/连线）时，才需要保存整份工作流 JSON，并会在恢复时校验与当前服务器版本的匹配度，不匹配就自动丢弃。
- 旧版 schema-1 草稿（整份 JSON 拷贝）在升级后自动作废清除，不再参与恢复。

# v0.1.50

- 新增"本地草稿"开关（设置 → 本地草稿），**默认关闭**：关闭后打开工作流永远直接读取服务器版本，不再保存、恢复或显示本地草稿，历史遗留的错误草稿也会被自动清除；需要"未保存修改自动恢复"（类似电脑浏览器）时可自行开启。
- 修复清除草稿后又被写回的问题：清除前会先取消待执行的草稿保存，并把当前工作流的内存状态一并复位（关闭开关时自动重新读取服务器版本），确保被旧版本弄乱的草稿不会再复活。

# v0.1.49

- 本地草稿增加内容校验：打开工作流时，如果手机本地草稿的节点结构与服务器当前工作流几乎不重合（说明是旧版本 bug 把其他工作流的数据混了进来），会自动丢弃该草稿并读取服务器版本，不再把错误数据带进参数页。
- 设置页新增"本地草稿"区域：显示本地草稿数量，可一键"清除全部本地草稿"，彻底清理历史遗留的混乱草稿；按工作流分别保存、打开时自动恢复的行为保持不变（与电脑浏览器草稿行为一致）。

# v0.1.48

- 修复高级编辑返回时读错工作流的问题：在编辑器里切到其他工作流后再关闭，之前会把其他工作流的数据写进当前工作流。现在关闭读取前会先校验当前激活工作流是否仍是打开的那一个，若不是，则用 ComfyUI 官方 `openWorkflow` 切回原工作流（保留编辑器内的修改）再读取，确保返回参数页的数据永远是当前工作流的。
- 保留 v0.1.47 的连线像素级检测与自动修复；不修改服务器和浏览器的连线显示设置，不修改工作流数据。

# v0.1.47

- 高级编辑连线消失继续加固：移除加载完成后那次多余的画布 resize + 强制重绘（转圈结束瞬间清线的首要嫌疑），改为打开脚本内部完成检测与修复；修复持续守护在上一次修复后可能停止重试的问题，现在每一帧都能自动恢复，直到状态真正健康。
- 新增连线"像素可见性"检测：除了统计画出的连线路径数，还会直接读取前景画布上连线位置的像素，判断连线是否真的显示出来（可区分"画了但 WebView 合成没显示"与"根本没画"）。
- 修复或检测到异常时，高级编辑器界面会直接显示"重绘前 / 重绘后 / 修复后 / 像素可见 / 修复方式"数字，截图即可反馈，不再需要翻日志。
- 保留 v0.1.46 的自动修复与诊断日志；不修改服务器和浏览器的连线显示设置，不修改工作流数据。

# v0.1.46

- 高级编辑连线修复升级为"加载后自动校验 + 自动修复"：打开工作流后先按与之前相同的刷新方式重绘一次，若连线在刷新后消失，自动按机制恢复（恢复可见连线模式，或临时使用经典槽位坐标重绘），并在编辑期间持续守护，不再只依赖单一前置设置。
- 诊断日志新增"重绘前 / 重绘后 / 修复后 / 修复方式"数值，可直接判断连线消失发生在数据还是绘制环节，以及命中的修复分支。
- 保留 v0.1.45 的经典节点画布切换；不修改服务器与浏览器的连线显示设置，不修改工作流数据。

# v0.1.45

- 修复高级编辑中连线短暂出现后全部消失：确认 ComfyUI Frontend 1.45.21 的现代节点模式会在 Android WebView 中卡于端口布局同步，官方 `drawConnections()` 因而主动跳过连线。
- App 内置 ComfyUI 网页在打开工作流前切换为与用户正常浏览器一致的经典节点画布，并等待现代节点 DOM 完全退出后再继续。
- 不修改工作流连线、不覆盖 `drawConnections()`，仍由 ComfyUI 自己加载和绘制完整工作流。

# v0.1.44

- 高级编辑改为复用 ComfyUI Frontend 1.45.21 工作流侧栏的原生打开流程：读取 `ComfyWorkflow.activeState`，并按原生参数调用 `loadGraphData`。
- 删除 App 对 `drawConnections`的接管、Vue 节点模式切换和帧内连线兼容绘制，高级编辑画布完全交还 ComfyUI 前端渲染。
- 已在本机安装的 ComfyUI Frontend 1.45.21 构建代码中核对侧栏 `openWorkflow()` 的真实调用顺序，不再猜测接口。

# v0.1.43

- 修复 v0.1.40 引入、并影响 v0.1.41/v0.1.42 的工作流解析脚本语法错误：高级编辑连线兼容逻辑多写了一个右括号，导致连接正常但任何工作流都无法进入参数页。
- 前端执行器增加独立脚本探针：页面 JavaScript 正常、但目标脚本没有启动时，会直接提示脚本语法或兼容性错误，不再误报成网页尚未稳定或反复重载页面。
- 保留 v0.1.42 的页面挂载、加载完成、串行恢复与本地草稿保护机制。

# v0.1.42

- 修复工作流页面已经显示加载 100%，却仍提示“ComfyUI 页面尚未稳定”而无法恢复最近工作流的问题：现在会同时等待 WebView 真正挂载、主页面完成回调和 JavaScript 回执。
- 前端脚本启动增加任务令牌，回执短暂丢失时不会重复执行同一份工作流；页面脚本持续无响应时会自动重载 ComfyUI 前端并重新读取当前工作副本。
- 连接、断线恢复、工作流读取、种子操作和参数刷新统一串行，避免后台重连刷新页面与用户打开工作流同时发生。
- 保留 v0.1.41 的本地草稿恢复逻辑；自动重载后仍使用手机当前工作副本，不会退回服务器旧参数。

# v0.1.41

- 每个 ComfyUI 服务器、每个工作流分别保存手机本地草稿；修改参数、表单布局或高级编辑后，即使 App 闪退、切到后台或 ComfyUI 重启，也能恢复当前工作副本。
- 服务器工作流同时发生变化时，不再静默覆盖；可选择继续手机草稿、读取服务器版本或另存为新工作流。
- 保存、另存、改名、移动和删除会同步维护本地草稿；本地最多保留最近 50 个工作流草稿。

# v0.1.40

- 撤销 v0.1.39 对 `Comfy.LinkRenderMode` 的错误修改：浏览器与 App 中的“隐藏链接”按钮都处于未启用状态，缺线并不是用户隐藏了连线；新版本不再改变服务器或浏览器的连线显示设置。
- 高级编辑改为等待全尺寸 WebView 真正可见后再载入工作流；当画布已经持有连线数据、连线模式可见，但 Android WebView 因 Vue 节点槽位布局未完成而一条路径都没有画出时，仅对该次绘制使用 LiteGraph 原生槽位坐标兼容显示连线。
- 修复 v0.1.39 偶发或持续提示“ComfyUI 页面正在加载”而无法打开工作流：前端脚本现在会等待页面地址和加载进度稳定，检测页面切换并重新初始化后重试。
- 工作流加载与生成、保存统一串行，避免快速点击或前端恢复标签时同时操作同一隐藏网页；诊断日志新增已载入连线数、已绘制路径数、Vue 节点状态和兼容绘制状态。

# v0.1.39

- 修复高级编辑虽然已经载入真实工作流及连线数据，但画布仍不显示连线的问题：此前只修改了 `canvas.links_render_mode`，随后会被 ComfyUI 的响应式设置重新覆盖为“隐藏”。
- 高级编辑现在会同步更新 ComfyUI 前端真实的 `Comfy.LinkRenderMode`，等待设置监听和节点插槽完成布局后再次应用可见模式，并强制重绘前景与背景画布。
- 诊断日志新增高级编辑连线检查，明确记录源工作流连线数、画布实际载入数、前端设置模式和画布模式；如果仍有设备差异，可以直接判断是数据未载入还是仅未绘制。

# v0.1.38

- 内置 ComfyUI 网页现在会等画布、工作流仓库和启动时的标签恢复全部稳定后，才打开目标服务器工作流，修复目标工作流随后被 `Unsaved Workflow (n)` 覆盖、标签名称错误的问题。
- “打开参数”改为先在手机内置网页中原生打开服务器工作流，再读取网页已经实例化的节点、控件、连线和组合子图生成原生参数页，不再只把 JSON 装进临时画布。
- 从高级编辑返回时，直接在编辑页面读取当前工作流 JSON、节点和参数清单并交回参数页；不会重新读取服务器旧文件，也不再在隐藏网页中重复装载一次。
- 原生打开完成后会校验 ComfyUI 当前活动标签的真实路径；若仍被其他启动流程覆盖，会显示期望路径和实际标签，而不是继续展示错误画布。

# v0.1.37

- 高级编辑改为复用 ComfyUI 网页工作流侧栏的真实打开链路：先从前端工作流仓库取得服务器上的 `ComfyWorkflow` 对象，再将该对象交给 `app.loadGraphData()`，不再把 JSON 重新装入临时画布。
- 网页原生工作流打开完成后，再逐项应用手机参数页尚未保存的控件值和回调；原始连线、组合子图、标签身份与服务器文件保持不变。
- 找不到对应服务器工作流或当前前端缺少官方工作流接口时会明确报错，不再悄悄退回到看似同名但没有正确连线状态的临时画布。

# v0.1.36

- LoRA 强度等浮点参数改为读取节点真实的 `FLOAT` 类型、范围和步进；即使当前值碰巧是整数，也不会再误判成整数，`0.5`、`0.01` 等小数可正常输入和生成。
- 高级编辑不再把当前 JSON 载入匿名空白画布，而是携带真实的服务器工作流路径打开；网页标签、工作流身份和保存目标对应原文件，同时保留手机端尚未保存的参数。
- 高级编辑会在原工作流存在连线时恢复可见连线；若工作流有连线但网页画布没有成功载入，会明确报错，不再显示成一堆来源不明的孤立节点。

# v0.1.35

- 配置本地保存后，生成结束不再提前弹“生成完成”；后台会继续等待白名单输出全部写入手机，只有本地保存完成或确认失败后才发提醒。
- 本地保存完成改用独立的高优先级通知通道，后台运行时可显示横幅提醒；进度通知继续保持低优先级且不会重复打扰。
- 点击进度或保存完成通知会复用当前应用页面，并恢复对应服务器、工作流和目标页面，不再把应用整体重启成空参数页。
- 参数页没有当前工作流时直接显示最近打开记录，冷启动或从通知进入后也能重新打开历史工作流。

# v0.1.34

- 修复新任务刚提交但尚未出现在 `/queue` 时，被错误切换成此前运行任务并显示“已接管”的问题；本次任务会保持选中并正常等待服务器确认。
- 应用重启或 WebSocket 重连后，兼容 ComfyUI 不带 `prompt_id` 的当前执行部件补发消息，仅关联本 App 已提交的运行任务，恢复参数页绿色流程框并继续跟随后续部件。
- 队列刷新改为与最新 WebSocket 状态合并，避免网络响应较慢时把已经前进的部件和进度倒退；工作流重新载入或参数切换后也会重新映射当前组合子节点。

# v0.1.33

- 修复 Android 16 上后台监控服务未及时建立前台通知而触发 `ForegroundServiceDidNotStartInTimeException` 的闪退；通知现在先于日志、锁和任务恢复建立。
- 工作流列表单击只选择并预读取参数；只有点击上方“打开参数”才写入最近历史并进入参数页。
- 参数页的最近工作流历史在服务器工作流列表尚未加载完成时也会立即显示，不再出现冷启动短暂空白。

# v0.1.32

- 修复同类节点中一个被设为“绕过”后，其上游仍在执行的正常节点也从参数页消失的问题。
- 执行链现在优先复用 ComfyUI 前端官方 `resolveInput()` 结果跨过绕过节点，组合子节点和多输入节点不再依赖物理连线猜测。
- 绕过节点继续不显示，但其前后正常节点的连接编号与颜色保持连续；旧版或自定义前端缺少官方解析能力时提供兼容回退。

# v0.1.31

- 工作流图片上传不再打开纵向文件列表，改用 Android 系统照片选择器，以相册缩略图网格选择图片。
- 视频上传同步改用系统媒体选择器并只显示视频；选择后仍直接上传原始媒体文件，不经过 App 压缩。
- 为支持 Google Play 服务的旧版 Android 声明照片选择器回移模块；设备确实不支持时才自动退回系统文件选择器。

# v0.1.30

- 针对“展开文本、先滑动、第一次唤起键盘后编辑位置跑开”的稳定复现路径修复外层自动定位。
- 多行文本首次获得焦点后的键盘动画期间，参数页暂时忽略系统发出的外层 `bringIntoView` 位移；键盘稳定后立即恢复正常自动滚动。
- 仅阻止首次键盘动画造成的页面搬动，不改变文本框内直接编辑、手动滚动、光标选择和后续输入行为。

# v0.1.29

- 撤销多行文本的全屏弹窗方案，恢复在参数部件内直接点击、定位光标和编辑。
- 参数部件列表不再使用会在键盘改变可视高度时回收部件的懒加载容器；输入框在首次唤起键盘时继续保留在组合树中，避免对象被销毁后丢失焦点。
- 输入期间由文本框本身持有文本、光标和选区，外部参数刷新只在未聚焦时同步，避免首次点击或移动光标时被参数状态覆盖。
- 执行节点仍通过主动滚动保持可见，不因取消懒加载列表而失去绿色执行框跟随。

# v0.1.28

- 高级编辑彻底改为独立原生页面：先把全尺寸 WebView 挂到窗口，再加载 ComfyUI 和当前工作流，不再复用参数页里长期隐藏的 1dp 桥接 WebView。
- 高级编辑页面保留加载进度与明确错误信息；WebView 渲染进程若被系统重建，新 WebView 会重新挂回可见容器，不再只剩黑色背景。
- 多行文本改为点击预览框后进入全屏编辑器，编辑器完成布局后再主动请求焦点和键盘；输入期间不再逐字重组参数列表，避免第一次点击丢焦点和滚动位置跳回顶部。
- 连线短横按完整标题区的实际高度居中，继续修正带白名单或执行状态文字时短横与编号的上下错位。

# v0.1.27

- 高级编辑不再放大长期处于隐藏尺寸的桥接网页；进入时新建全尺寸内置 WebView，加载当前 ComfyUI 服务器首页并重新载入当前工作流，关闭后自动刷新原生参数。
- 高级编辑增加打开进度、失败提示和顶部安全区，加载失败会返回参数页，不再停留在空白黑屏。
- 连线短横改为测量每个部件标题区和数字列的实际高度后居中定位，修复不同标题行数、本地白名单和执行状态造成的上下错位。

# v0.1.26

- 修复参数页连线短横与数字上下错位：短横现在复用对应数字的实际字体行高和垂直中心，系统字体缩放后仍保持一一对齐。
- 左侧输入短横和右侧输出短横分别锚定部件边框，数字长度不同也不会改变短横位置。

# v0.1.25

- “另存”新增服务器目录选择器，可保存到工作流根目录或已有的多层中文子目录，成功后自动切换到新工作流。
- 多行文本的光标和选区提升到工作流页面保存；部件折叠再展开后，唤起键盘不再把编辑位置重置到开头。
- 部件连线标识取消圆点，改为与网页端类型颜色一致的短横线；短横中心跨在部件左右边框上，数字仍跟随对应连线颜色。
- 当前工作流没有匹配的已启用本地输出白名单时，点击生成会先弹窗说明本地不会缓存，并允许取消或明确选择“仍然生成”。

# v0.1.24

- 完全取消手动连接的 IP 网段限制，可填写局域网、VPN、共享地址、公网 IP 或域名；`http://100.64.0.10:18188` 可直接使用。
- 继续校验 HTTP 协议、主机格式和端口范围；HTTPS、空主机和非法端口仍会明确拒绝。
- 连接页和 README 更新安全说明：ComfyUI 默认没有认证，允许公网地址不代表可以把 8188 端口直接暴露到公网。

# v0.1.23

- 移除参数列表重复叠加的键盘高度，窗口统一使用系统 `adjustResize`，修复键盘弹出后文本框下方出现大块空白遮罩的问题。
- 取消延迟强制移动整个输入框，长文本改为独立保存光标与选区；输入引起界面重组时不再把编辑位置重置到开头。
- 多行文本框由固定 140dp 改为宽高 `2:1` 自适应比例，例如宽 1600 时高度为 800，横竖屏都会随可用宽度调整。

# v0.1.22

- 保存工作流不再执行完整的 `graphToPrompt()`；保存与生成共用串行桥接锁，并在保存结束前禁用生成，修复保存后快速点击生成时隐藏 WebView 偶发崩溃。
- 文本、数字和文件参数获得焦点后，会在键盘完成弹出后把实际输入框移入可视区，不再把展开部件跳回顶部。
- WebSocket 客户端身份改为安装内固定保存；重启 App 后会从服务器队列找回本 App 已提交的运行中/待执行任务，恢复前台状态并重新启动后台监控。
- 任务列表会从队列和历史附带信息恢复真实工作流名称，接管任务时不再只显示空名称。
- 原生组合子图的 `68:76` 一类内部执行编号会映射回参数页外层部件 `68`，放大流程可以持续显示外层绿色执行框。
- 快速执行的对比等部件增加 450 毫秒最短绿色提示时间，避免节点瞬间执行完导致肉眼完全看不到绿框。

# v0.1.21

- 参数页顶部工作流选择框缩短并压低高度，名称、部件数和参数数改用更紧凑的字号与间距。
- 横竖屏旋转改为原地更新布局，不再销毁并重建隐藏的 ComfyUI WebView；生成期间旋转不会重新进入参数整理。
- 生成入口增加单任务保护，旋转或连续点击都不会重复提交生成请求。
- 部件原有大框保持不变，展开后在内部增加一个浅色小框统一包住该部件的全部参数，并缩窄参数区留白，明确区分部件标题、参数名称和参数值。
- 移除所有整数和小数参数的滑杆，只保留准确的数字输入，避免不合理范围导致难以调整。

# v0.1.20

- 参数页左上工作流信息改为带边框的可点击选择框，点击后可从最近打开的 10 个工作流中快速切换。
- 最近工作流列表持久保存，并兼容旧版“最后打开工作流”记录；改名、移动和删除后会同步更新。
- 原“保存工作流”长按钮改为同一圆角框内平分的“保存 / 另存”；另存支持输入新名称，并将当前参数完整写入新工作流。
- 另存会生成新的工作流 ID，拒绝覆盖同名文件，成功后自动切换到新工作流。

# v0.1.19

- 修复原生组合子图展开后，App 按根画布节点 ID 误删内部 Prompt 节点，导致 `SaveImage`、`Image Comparer` 等下游部件同时出现 `Exception when validating node`、无法生成的问题。
- 执行链筛选改为从有效输出反向保留全部依赖；未连接分支仍会排除，组合子图内部依赖则完整保留。
- 本地保存白名单继续只负责生成完成后的手机端保存，不参与 ComfyUI Prompt 节点取舍，也不会影响服务器端 `SaveImage`。

# v0.1.12

- 设置新增诊断日志开关，记录连接、工作流加载、生成提交、后台保存以及未捕获闪退；支持应用内查看、导出和清空，不记录提示词、工作流正文或生成图片。
- 本地保存白名单改为按“服务器 + 输出部件类型”对所有工作流生效；旧的逐工作流规则会自动合并，不再需要每个工作流重复设置。
- 从任务附带的真实工作流 JSON 解析输出部件类型，前台与后台保存使用同一匹配规则，多批次输出仍逐项保存。
- 工作流页改为文件夹浏览：根页面只显示根目录文件和分类文件夹，进入文件夹后只显示直接子项；搜索仍覆盖全部分类。

# v0.1.11

- 项目已迁移到独立目录 `C:\Users\liu\Documents\COMFYUI_app`，保留完整 Git 历史，并继续使用仓库外的正式签名。
- 工作流页删除双击和长按手势；选中后使用“打开参数、新建副本、改名、移动、导出、删除”等中文文字按钮。
- 工作流保存只保留在参数页；服务器已有同名工作流时，明确询问是否强制覆盖，服务器文件被其他设备修改时给出额外警告。
- 顶部刷新会先检查服务器，离线或检查失败时执行完整重连，不再在非连接状态直接失效。
- 后台任务服务及进度更新增加系统异常隔离，任务已经提交后不会因前台服务调用异常带崩 App；后台服务仍独立轮询并保存白名单输出。

# v0.1.10

- 软件更新下载按 `ghfast → ghproxy → GitHub 原地址` 探测，优先选择可用的国内加速节点，安装前仍校验 SHA-256、包名、版本和签名。
- 图片详情始终隐藏底部手势小白条，并保留导航栏内边距兼容兜底；保存/下载会当地显示进行中、完成或失败。
- 结果页在底部页签切换后保留本地/云端、平铺/任务相册以及当前具体相册位置。
- 沉浸浏览两侧增加上一张/下一张按钮；放大后换图保留相同缩放倍数和中心偏移。

# v0.1.9

- 任务相册数量从图片角标移到日期行右侧，不再遮挡缩略图。
- 单图和任务相册支持长按多选、全选、批量下载/保存；本地作品支持按图或按整册删除。
- 图片详情页新增分享、收藏、下载/保存、删除和更多工具栏，收藏状态会持久保存。
- 单击图片切换沉浸浏览；双指放大或双击放大时自动进入沉浸模式，再次双击恢复初始比例。

# v0.1.8

- 本地保存与云端媒体列表彻底拆开：前台服务按本 App 的具体 `prompt_id` 读取完整任务输出数组，多图逐张保存。
- App 退到后台后，前台服务继续等待生成、保存全部白名单输出并更新本地索引，全部完成后才停止并通知保存数量。
- 后台监控使用局域网已连接设备前台服务，任务期间持有 CPU 和 Wi-Fi 锁，息屏或切到后台也会继续跑完。
- 页面 WebSocket 不再在任务成功时提前停止前台服务；云端页刷新不再触发本地历史回填。

# v0.1.7

- 参数页保留执行链路中的全部部件，包括 VAE 解码等没有可调参数的部件。
- 补齐 rgthree Seed 部件的网页快捷操作：每次随机、新固定随机和使用上次排队种子。
- 修复云端媒体切换“全部平铺”时因重复文件地址导致的闪退。
- 高级编辑改为带固定顶部关闭栏的内置 ComfyUI 网页；系统返回键和关闭按钮均可退出，关闭后自动刷新工作流参数。
- 本地结果明确改称“本地作品”，实体文件独立于云端；删除前显示规则确认，删除后旧历史不会自动复活，新任务仍按白名单保存。

# v0.1.6

- 修复任务完成后停在“部件 null”、结果页和本地缓存不刷新的问题。
- 连接编号改为每种连接颜色独立计数；同一输出分叉仍使用 `a/b/c` 后缀。
- 部件展开和收起改用常见的折叠图标。
- 兼容 ComfyUI 先发送成功事件、后写入历史的时序，批量输出会在历史落盘后完整同步。

# v0.1.5

- 增加 ComfyUI 0.28 的 `progress_state` 实时进度支持，顶部和参数页显示当前部件及百分比。
- 正在执行的参数部件显示绿色边框并自动滚动到屏幕内，执行过程会像网页端一样逐个部件移动。
- 生成完成、失败、中断和取消均有明确状态，并及时刷新结果和停止任务通知。
- 工作流列表改为：单击选择、双击进入参数、长按保存；导入后不再因单击自动跳页。
- 参数部件左右两侧新增彩色输入/输出连接点与数字标记；分叉连接以下游 `4a/4b/4c` 标记表示同属上游输出 `4`。
- 修复批次结果并发写入时本地相册可能只保留最后一张的问题；升级后刷新结果会补齐此前缺少的同批图片。
- 图片改用全屏图库查看，支持双指缩放、放大后拖动和左右滑动切换图片。

安全提醒：本应用只面向可信局域网中的无认证 ComfyUI，不要把 8188 端口暴露到公网。
